export function exec({ $mongo: { $db } }: {
    $mongo: {
        $db: any;
    };
}, { hostname, domainId }: {
    hostname: any;
    domainId: any;
}): any;
export function exec({ $mongo: { $db } }: {
    $mongo: {
        $db: any;
    };
}, { hostname, domainId }: {
    hostname: any;
    domainId: any;
}): any;
