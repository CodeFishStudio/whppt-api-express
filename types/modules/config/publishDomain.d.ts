export function exec({ $mongo: { $publish } }: {
    $mongo: {
        $publish: any;
    };
}, { domain }: {
    domain: any;
}): any;
export function exec({ $mongo: { $publish } }: {
    $mongo: {
        $publish: any;
    };
}, { domain }: {
    domain: any;
}): any;
