export function exec({ $mongo: { $db } }: {
    $mongo: {
        $db: any;
    };
}, { slug, _id, collection, domainId }: {
    slug: any;
    _id: any;
    collection: any;
    domainId: any;
}): any;
export function exec({ $mongo: { $db } }: {
    $mongo: {
        $db: any;
    };
}, { slug, _id, collection, domainId }: {
    slug: any;
    _id: any;
    collection: any;
    domainId: any;
}): any;
