export function exec({ $mongo: { $db } }: {
    $mongo: {
        $db: any;
    };
}, { redirect }: {
    redirect: any;
}): any;
export function exec({ $mongo: { $db } }: {
    $mongo: {
        $db: any;
    };
}, { redirect }: {
    redirect: any;
}): any;
