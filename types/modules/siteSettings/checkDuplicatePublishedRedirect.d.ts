export function exec({ $mongo: { $dbPub } }: {
    $mongo: {
        $dbPub: any;
    };
}, { redirect }: {
    redirect: any;
}): any;
export function exec({ $mongo: { $dbPub } }: {
    $mongo: {
        $dbPub: any;
    };
}, { redirect }: {
    redirect: any;
}): any;
